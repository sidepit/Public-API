# TUI dependencies (the SDK's own deps live in python-client/requirements.txt;
# easiest is to install these INTO python-client/.venv and run with it)
textual>=8.0
qrcode>=7.0
# optional — only the LOCK (deposit broadcast) money path needs it:
# bitcoinlib
